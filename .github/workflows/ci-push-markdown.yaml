# CI workflow for Markdown files
name: ci-push-markdown

on:
  push:
    branches:
      - "main"
      - "master"
      - "hotfix/**"
      - "release/**"
      - "develop"
      - "feature/**"
    paths:
      - "**/*.md"
      - ".markdownlint.yaml"
      - ".github/workflows/ci-push-markdown.yaml"
      - ".github/workflows/reusable-markdownlint.yaml"
  pull_request:
    branches:
      - "main"
      - "master"
      - "hotfix/**"
      - "release/**"
      - "develop"
      - "feature/**"
    paths:
      - "**/*.md"
      - ".markdownlint.yaml"
      - ".github/workflows/ci-push-markdown.yaml"
      - ".github/workflows/reusable-markdownlint.yaml"

jobs:
  markdown-lint:
    uses: ./.github/workflows/reusable-markdownlint.yaml
    with:
      environment: "markdown"
      component: "base"
    permissions:
      contents: read
      pull-requests: write
